## 🔍 前后端验证测试计划

### 阶段 1：后端 API 测试

1. **启动后端服务**

   * 运行 `python run.py` 启动 Flask 应用（端口 5000）

2. **逐个测试 7 个 API 接口**

   ```bash
   curl http://127.0.0.1:5000/getAllData/getHomeData
   curl http://127.0.0.1:5000/getAllData/getTableData
   curl http://127.0.0.1:5000/getAllData/getArticleData
   curl http://127.0.0.1:5000/getAllData/getCommentData
   curl http://127.0.0.1:5000/getAllData/getIPData
   curl http://127.0.0.1:5000/getAllData/getYuqingData
   curl http://127.0.0.1:5000/getAllData/getContentCloudData
   ```

3. **记录每个 API 的返回数据结构**

### 阶段 2：前端配置检查

1. **验证 Vite 代理配置** - 确认代理指向正确
2. **检查前端 API 调用** - 验证请求路径
3. **检查数据映射** - 确认前端组件正确解析返回数据

### 阶段 3：页面功能验证

1. **启动前端服务** - `cd frontend && npm run dev`
2. **逐个访问页面** - 验证数据加载

   * 首页 `/`

   * 热词分析 `/analysis/hotWords`

   * 文章分析 `/analysis/article`

   * 评论分析 `/analysis/comment`

   * IP 分析 `/analysis/ip`

   * 舆情分析 `/analysis/sentiment`

   * 词云 `/analysis/wordCloud`

### 阶段 4：生成验证报告

* 记录每个 API 的测试结果

* 标注字段映射关系

* 列出发现的问题和建议修复方案

